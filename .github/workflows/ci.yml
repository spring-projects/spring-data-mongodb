name: CI Build

on:
  workflow_dispatch:
  push:
    branches: [ 5.0.x, 'issue/5.0.x/**' ]

permissions: read-all

jobs:
  build-java:
    strategy:
      matrix:
        java-version: [ base, main ]
        mongodb-version: [ 'latest', '8.2', '8.0', '7.0' ]
    name: Build project
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Setup Java and Maven
        uses: spring-projects/spring-data-build/actions/setup-maven@4.0.x
        with:
          java-version: ${{ matrix.java-version }}
          develocity-access-key: '${{ secrets.DEVELOCITY_ACCESS_KEY }}'
      - name: Setup MongoDB Server
        uses: spring-projects/spring-data-build/actions/setup-mongodb@4.0.x
        with:
          version: '${{ matrix.mongodb-version }}'
          replica-set: 'rs0'
      - name: Build
        uses: spring-projects/spring-data-build/actions/maven-build@4.0.x
